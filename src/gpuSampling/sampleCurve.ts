import { Vec4 } from "wgpu-matrix";



export const sampleCurve = async function(weightedControls: Vec4[], knots: number[], degree: number): Promise<GPUBuffer> {

  // GPU Accelerated Sampling :)
  throw new Error("Todo");

}
