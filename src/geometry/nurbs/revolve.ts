import { Vec3 } from "wgpu-matrix";
import { Curve } from "./curve";

export const revolve = function(origin: Vec3, xAxis: Vec3, yAxis: Vec3, thetaStart: number, thetaEnd: number, curve: Curve): Curve {
  throw new Error("not implemented");
}
