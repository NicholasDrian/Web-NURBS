import { Vec3 } from "wgpu-matrix";


export const parsePointInput = function(input: string): Vec3 | null {
  const components: string[] = input.split("");
  // TODO: learn to split with regex
  return null
}
